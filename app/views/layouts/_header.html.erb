<nav class="navbar navbar-toggleable-md bg-inverse">
  <div class="container">
    <div class="navbar-header">
      <%= link_to 'Smarth Health Application', root_path, class: 'navbar-brand' %>
    </div>
    <div class="navbar-collapse collaspse" id="navbarNavDropdown">
      <ul class="nav navbar-nav mr-auto">
        <% if user_signed_in? %>
          <li><%= link_to "Doctors", doctors_path, class: "nav-link" %></li>
          <li><%= link_to "Patients", patients_path, class: "nav-link" %></li>
        <% end %>
      </ul>
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
              <%= current_user.email %>
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to 'Profile', edit_user_registration_path %></li>
              <li><%= link_to 'Log out', destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
        <ul class="nav navbar-nav">
          <li><%= link_to 'Log In', new_user_session_path, class:'navbar-brand' %></li>
          <li><%= link_to 'Sign Up', new_user_registration_path, class: 'navbar-brand' %></li>
          <% end %>
        </ul>
      </ul>
    </div>
  </div>
</nav>
<br>
</body>
</html>
